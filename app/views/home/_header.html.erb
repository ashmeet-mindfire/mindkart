<nav class="navbar bg-body-tertiary" data-bs-theme="dark">
    <div class="container-fluid">
        <div class="d-flex gap-4">
            <a class="navbar-brand">MindKart</a>
            <ul class="navbar-nav d-flex flex-row gap-4">
                <li class="nav-item"><%= link_to 'Shop', shop_path, class: "nav-link" %></li>
            </ul>
        </div>
        <ul class="navbar-nav d-flex flex-row gap-4">
            <% if user_signed_in? %>
                <% if current_user.admin? %>
                    <li class="nav-item"><%= link_to 'Admin Dashboard', admin_path, class: "nav-link" %></li>
                <% end %>
                <li class="nav-item">
                <%= link_to 'Logout', destroy_user_session_path, method: :delete, data: { turbo: false, confirm: "Are you sure you want to log out?" }, class:"nav-link" %></li>
            <% else %>
                <li class="nav-item"> <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %></li>
                <li class="nav-item"><%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link' %></li>
            <% end %>
        </ul>
    </div>
</nav>